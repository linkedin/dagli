/**
 * Loss functions compute the loss (error) of a given layer relative to the true label during training.
 */
@Visitor(name = "LossFunctionVisitor", isPublic = true)
package com.linkedin.dagli.nn.loss;

import com.linkedin.dagli.annotation.visitor.Visitor;